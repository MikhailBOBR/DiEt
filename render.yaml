services:
  - type: web
    name: nutritrack-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    preDeployCommand: npm run migrate
    healthCheckPath: /api/ready
    envVars:
      - key: APP_ENV
        value: production
      - key: SERVER_PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: DB_PROVIDER
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: nutritrack-db
          property: connectionString

databases:
  - name: nutritrack-db
    databaseName: nutritrack
    user: nutritrack
